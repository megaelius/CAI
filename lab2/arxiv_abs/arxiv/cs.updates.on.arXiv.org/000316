Let $\mathcal{A}$ and $\mathcal{A}_i$, $i=1,2$, be three \textit{measure many quantum automata}(MMQFAs)
over alphabet $\Sigma$, $0<\lambda\leq 1$, and $\mathcal{P}_{\mathcal{A}}(\omega)$ the
probability accepting $\omega\in\Sigma^*$ of $\mathcal{A}$. Then the strict (resp. non-strict)
cut-point language $L_{>\lambda}(\mathcal{A})$ (resp. $L_{\geq\lambda}(\mathcal{A})$)
recognized by $\mathcal{A}$ are defined as the set $\{\omega\in\Sigma^*|\mathcal{P}_{\mathcal{A}}(\omega)>\lambda\}$
(resp. the set $\{\omega\in\Sigma^*|\mathcal{P}_{\mathcal{A}}(\omega)\geq\lambda\}$),
the equivalence of strict (resp. non-strict) cut-point languages is defined to ask whether or not
$L_{>\lambda}(\mathcal{A}_1)=L_{>\lambda}(\mathcal{A}_2)$ (resp. $L_{\geq\lambda}(\mathcal{A}_1)=L_{\geq\lambda}(\mathcal{A}_2)$),
and the strict (resp. non-strict) emptiness problem is to decide whether $L_{>\lambda}(\mathcal{A})=\emptyset$
(resp. $L_{\geq\lambda}(\mathcal{A})=\emptyset$). In this paper, we will show the following
\begin{itemize} \item {the so-called \textit{measure once quantum automata}(MOQFA) is a special
kind of measure many quantum automata (Proposition \ref{lemc});} \item {we provide an alternative
proof about the undecidability of emptiness about measure many quantum finite automata (Corollary
\ref{corf}, Corollary \ref{cora});} \item {the equivalence of strict (resp. non-strict) cut-point
language recognized by measure many quantum automata is undecidable, which clarifies an author's
misapprehended corollary (Corollary \ref{corh}). This also implies that the containment problem
is undecidable (Corollary \ref{cory}, Corollary \ref{corx}).} \end{itemize} 